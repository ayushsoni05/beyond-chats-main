# Beyond-chats
BeyondChats Multi-Phase Solution – A full-stack application that scrapes BeyondChats blog articles, refreshes their content via Google search and LLM rewriting, and displays both original and updated versions. Built with Laravel backend (scraping &amp; CRUD),Node.js pipeline (search,scrape competitors,LLM rewrite), and React frontend (article display).
